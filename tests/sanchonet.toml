# SanchoNet Test Configuration
# Test configuration for full integration testing against SanchoNet

data_dir = "./test-data/sanchonet"
gap_limit = 20

[api]
enabled = true
bind = "127.0.0.1:50052"  # Different port for tests

[networks.sanchonet]
enabled = true
relay = "sanchonet-node.world.dev.cardano.org:30004"
magic = 4
# Genesis hash for SanchoNet (Conway era)
# This is the governance testnet for testing CIP-1694 features
